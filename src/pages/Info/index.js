import { SafeAreaView, View,Text } from 'react-native';

export default function Info() {
 return (
   <SafeAreaView style={{flex: 1, justifyContent: 'center', alignItems: 'center', }}>
    <Text style={{fontSize: 50, color: '#000', }}>EM BREVE</Text>
   </SafeAreaView>
  );
}